server:
  port: 9002

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/db?serverTimezone=UTC&characterEncoding=UTF-8
    username: username
    password: password

mybatis:
  config-location: classpath:config/mybatis-config.xml 
  mapper-locations: classpath:mapper/**/*.xml